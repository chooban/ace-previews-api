machine:
  services:
    - docker
  node:
    version: 4.4.4

deployment:
  tutum:
    branch: master
    commands:
      - docker build -t chooban/previews-service .
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASSWORD
      - docker push chooban/previews-service:latest
